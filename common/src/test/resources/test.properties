resources.folder=classpath:

bestallning.text-resource-path=${resources.folder}/texter/bestallning/*.xml
bestallning.image-resource-path=${resources.folder}/images/
bestallning.host=${ib.host.url}

mail.text-resource-path=${resources.folder}/texter/mail/*.xml

activemq.broker.url=vm://localhost?broker.persistent=false
activemq.broker.username=
activemq.broker.password=
