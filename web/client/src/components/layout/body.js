import styled from 'styled-components'
import ibValues from '../style/IbValues'

const BodyCenterWrapper = styled.section`
  margin: auto;
  max-width: ${ibValues.maxContentWidth};
  padding: 20px;
`;

export default BodyCenterWrapper;
