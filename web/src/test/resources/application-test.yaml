activemq:
  broker:
    url: vm://localhost?broker.persistent=false
    password:
    username:

pdl:
  logging:
    system-id: devtest-id
    system-name: devtest-name
    queue-name: ib.pdllogging.queue

spring:
  main:
    allow-bean-definition-overriding: true
